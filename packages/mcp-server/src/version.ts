// One place for the running server's version. It is not only what the panel
// prints: an answer this server computes and hands to the plugin to cache is
// stamped with it, so a later server can tell whether the cached answer was
// worked out by the code it is running or by some other release.
export const VERSION = "0.7.4";
